# 라스트 테이크 v1.4.3 검증 결과

검사일: 2026-09-12 · 제작자: 도구리

## 이번 수정본으로 실행한 결과

| 검사 | 통과 | 실패 |
|---|---:|---:|
| Node.js 전체 로직·구조 검사 | 105 | 0 |
| 이번 선택창·섭외 교체 브라우저 검사 | 157 | 0 |
| 기존 모바일 화면 회귀 검사 | 84 | 0 |
| 브라우저 확인 합계 | 241 | 0 |

감지된 JavaScript 런타임 오류는 0개입니다. 새 브라우저 검사는 4개 화면 크기에서 실행했고, 회귀 검사는 8개 화면 크기에서 실행했습니다. 겹치는 크기나 중간 디버깅 실행을 중복 집계하지 않았습니다. 이전 버전 이름의 다른 보고서는 과거 기록이며 이번 합계에 포함하지 않았습니다.

상세 결과는 `tests/reports/node-tests.txt`, `mobile-v143-report.json`, `v143-regression-mobile-v142.json`, `summary.json`에 있습니다. 회귀 보고서에는 실행한 앱 버전과 기존 검사 스크립트 버전을 구분해 기록했습니다.

## 모바일 세부장르 선택창

선택을 변경해도 네이티브 select DOM 노드가 그대로이며 정확히 3개만 존재하는지 검사했습니다. 선택 직후 select가 추가·제거되지 않고 다른 select로 포커스를 다시 옮기지 않으며, 터치 환경에서는 선택 완료 후 포커스를 정리합니다. 세부장르 설명과 조합 효과는 실제 선택값에 맞게 갱신됩니다.

모달 스크롤 위치를 보존하고, 화면 높이 변경이나 배경 목록의 높이 재계산이 열린 기획창을 다시 만들지 않는 것을 검사했습니다. 320픽셀 폭과 가로 방향에서도 3개 선택창이 같은 줄에 남습니다. PC에서는 키보드 선택의 포커스를 유지합니다. 다음 단계로 이동했다가 돌아와도 선택한 조합은 그대로입니다.

## 최종 섭외 조건의 인물별 교체

감독 1명, 주연 2명, 조연 4명 전부에 교체 버튼이 표시됩니다. 참여 수락·추가 요구·거절 상태에 관계없이 교체 가능하며, 선택한 시나리오에 연결된 작가는 그대로입니다.

교체 후보 선택은 임시 미리보기입니다. 후보를 열거나 취소할 때 기존 인물, 수락 기록, 현금, 거래 기록과 날짜는 변하지 않습니다. 이미 선정한 인물, 다른 직군, 촬영 중이거나 활동 조건에 맞지 않는 인물을 차단합니다. 거절한 후보를 강제로 수락하지 못합니다.

후보를 수락하면 지정한 자리만 교체하고 감독·주연·조연 7명의 새 시나리오·감독·작가·동료·제작사 관계 조건을 다시 계산합니다. 7개 자리를 차례로 교체하면서 화면의 모든 상태·추가 비용·합계가 새 계산과 일치하는지 확인했습니다. 이전 인물 조합의 서명을 무효화하며, 새 전체 조건을 확인해야 제작을 시작할 수 있습니다. 관계가 바뀌어도 반올림·조건 구간에 따라 일부 금액은 같을 수 있습니다.

후보 답장의 취소·뒤로가기, 교체 목록의 닫기와 브라우저 뒤로가기가 각각 후보 목록과 최종 섭외 목록으로 돌아오는지 확인했습니다. 교체 후 닫았다가 다시 열어도 새 조합을 보존합니다. 최종 조건을 수락하기만 해서는 돈이 나가지 않으며, 제작 시작 시 새 견적에 해당하는 금액이 한 번만 차감됩니다. 제작 후 뒤로가기에서 이전 기획이 살아나 중복 계약되지 않도록 검사했습니다.

## 유지한 동작과 저장

첫 화면의 띄어쓰기와 한 화면 맞춤, 모바일 시나리오의 전체 제안 세로 스크롤, 마지막 기획 버튼과 하단 메뉴 사이 여백, 인물 목록 페이지 방식, 세부장르 3열 배치, 특수상영 견적과 러닝타임 가중, 저장하고 첫 화면·이어서 플레이, 원본 인물 이미지 로딩을 회귀 검사했습니다. 이번 기능 검사에서 더보기 → 가이드 → 확인 → 더보기 → 닫기 → 게임 복귀도 확인했습니다.

저장 키와 스키마, 이전 계약과 실적, 시장·세금·특수상영 계산식과 원본 이미지 파일은 변경하지 않았습니다. 교체·브라우저 이동 때문에 이미 진행한 날짜나 자금이 되돌아가지 않습니다.

## 실행한 명령과 화면 크기

```sh
npm test
python tests/mobile-v143-browser.py --output /path/to/qa-v143
python tests/mobile-v142-browser.py --output /path/to/regression-v142
```

새 선택창/교체 UI: 320×568, 390×844, 568×320, 1366×768.

회귀 검사: 320×568, 360×640, 390×844, 412×915, 568×320, 844×390, 768×1024, 1366×768.

## 검사 방법과 한계

Chromium과 Playwright에서 실제 dist 모듈·스타일·이미지를 기존 오프라인 blob 방식으로 전달해 실행했습니다. 테스트용 가상 제작사와 저장 데이터를 사용하지만 게임 로직과 Chromium DOM·History API는 대체하지 않았습니다. 이 환경의 HTTP 브라우저 이동은 관리자 정책으로 차단되어 배포된 주소를 열어 확인하지 않았습니다. agent-browser CLI도 사용할 수 없어 기존 프로젝트의 오프라인 브라우저 검사기를 사용했습니다.

실제 iPhone Safari·Android 기기와 운영체제의 네이티브 선택 패널은 검사하지 못했습니다. 이번 결과는 앱이 select를 재생성·재포커스하지 않는 동작과 모바일 화면 크기의 UI 검증이며, 모든 실기기에서 네이티브 패널 증상이 재현되지 않음을 확정하는 검사는 아닙니다.

GitHub 원격 푸시와 기존 실행 주소 재배포는 수행하지 않았습니다. 배포 시 dist 전체를 반영하고 기존 사이트 저장 데이터를 보존하세요.
